test-docker:
  stage: test
  image: docker:26
  variables:
    DOCKER_HOST: unix:///var/run/docker.sock
    DOCKER_TLS_CERTDIR: ""
  script:
    - ls -l /var/run/docker.sock
    - docker version
    - docker ps
